export function H1({ children }: { children: React.ReactNode }) {
  return <h1 className="tracking-tight">{children}</h1>;
}
export function H2({ children }: { children: React.ReactNode }) {
  return <h2 className="tracking-tight">{children}</h2>;
}
